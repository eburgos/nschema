[<DataContract>]
type {{= name }} =
    {{
    var $cnt = 0;
    for(var prop in properties) {
    }}  // {{- properties[prop].description || '' }}
    | [<EnumMember>][<JsonProperty("{{- prop }}")>] {{- $nschema.utils.initialCaps(prop) }} = {{= $cnt }}
    {{ $cnt += 1; } }}