<% include classHeader %>
export <% if ($subType === 'enumeration') {%><% include enum %><% }
else {
 %>interface <%= name %> <% if ((typeof(implements) !== 'undefined') && implements.length) { %>extends <%
    (implements || []).forEach(function (i) {
    	if (i > 0) {
    		%>, <%
	    }
        var $nschemaType = i;
%><% include ../typeName %><%
    });
}
i = 1;
%> {
<% for(var prop in properties) {
        var $property = properties[prop];
        var $nschemaType = properties[prop].type;
        var $registeredType = $nschema.getObject($nschemaType.namespace || namespace || '', $nschemaType.name) || $nschemaType;
%><%- $u.i(i, '  ') %>/**
<%- $u.i(i, '  ') %> * <%- properties[prop].description || '' %>
<%- $u.i(i, '  ') %> */
<%- $u.i(i, '  ') %><%- properties[prop].typescriptName || prop %><%- properties[prop].type.nullable?'?':''%>: <% include ../typeName %>;
<% } %>
}<% } %>