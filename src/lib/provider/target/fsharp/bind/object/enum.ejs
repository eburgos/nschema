[<DataContract>]
type <%= name %> =
    <%
    var $cnt = 0,
        $currentVal;
    for(var prop in properties) {
        if (typeof(properties[prop].fsharpValue) !== 'undefined') {
            $currentVal = properties[prop].fsharpValue;
        }
        else {
            $currentVal = $cnt;
        }
    %>  // <%- properties[prop].description || '' %>
    | [<EnumMember>][<JsonProperty("<%- prop %>")>] <%- properties[prop].fsharpName ||  $nschema.utils.initialCaps(prop) %> = <%= $currentVal %>
    <% $cnt += 1; } %>